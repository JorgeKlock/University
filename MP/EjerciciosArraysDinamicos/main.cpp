/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   main.cpp
 * Author: jorgeklock
 *
 * Created on 3 de abril de 2019, 17:53
 */

#include "problema1.h"
const int FIN=-1;
/*
 * 
 */
int main(int argc, char** argv) {

    /*  PROBLEMA 1
    int llenos=0;
    int TAM;
    cout << "Tamano inicial del vector: ";
    cin >> TAM;
    int *p=new int[TAM];
    int valor=0;
    while(valor!=FIN)
    {
        cout << "Introduzca valor: ";
        cin >> valor;
        if(valor!=FIN)
        {
            Anadirdato(p, valor, llenos, TAM);
            Mostrarvector(p, TAM);
            llenos++;
        }
    };

    delete [] p;
     */
    /*
    int tam;
    int* v=new int[5];
    v[2]=7;
    v[4]=3;
    
    int* sol;
    obtenerSegmento(v, 5, sol, tam);
    cout << "Vector solucion: ";
        Mostrarvector(sol, tam);
    delete[]v;
    delete[]sol;
    */
    return 0;
}

